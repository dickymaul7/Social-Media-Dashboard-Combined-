import {NextResponse} from "next/server";
import {compactJson,createStructuredJson} from "@/lib/ai/core";

export const dynamic="force-dynamic";export const maxDuration=120;
const schema={type:"object",required:["overall_score","verdict","audience_fit","positioning_alignment","capability_relevance","market_relevance","claim_compliance","strengths","misalignments","recommendations"],properties:{overall_score:{type:"integer",minimum:0,maximum:100},verdict:{type:"string"},audience_fit:{type:"integer",minimum:0,maximum:100},positioning_alignment:{type:"integer",minimum:0,maximum:100},capability_relevance:{type:"integer",minimum:0,maximum:100},market_relevance:{type:"integer",minimum:0,maximum:100},claim_compliance:{type:"integer",minimum:0,maximum:100},strengths:{type:"array",items:{type:"string"}},misalignments:{type:"array",items:{type:"string"}},recommendations:{type:"array",items:{type:"string"}}}};
export async function POST(request:Request){try{const body=await request.json().catch(()=>({}));if(!body?.brief||!body?.brandProfile)return NextResponse.json({ok:false,error:"Full Brief dan Brand Intelligence wajib tersedia."},{status:400});const result=await createStructuredJson<any>({schema,system:"Kamu adalah senior B2B brand governance dan editorial alignment reviewer. Bandingkan content brief dengan Brand Intelligence secara ketat. Jangan menganggap brand aligned hanya karena nama brand disebut.",user:`BRAND INTELLIGENCE / GUARDRAILS:\n${compactJson(body.brandProfile)}\n\nFULL STORYTELLING BRIEF:\n${compactJson(body.brief)}\n\nNilai: audience fit, positioning alignment, capability relevance, market relevance, dan claim compliance. Claim compliance harus mengecek allowed/prohibited claims dan evidence boundary. Overall score bukan rata-rata kosmetik; penalti keras jika Brand POV menyimpang atau claim tidak didukung. Berikan strengths, misalignments, dan recommended actions yang konkret.`,temperature:.12});return NextResponse.json({ok:true,data:result})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Brand Alignment QC gagal."},{status:500})}}
